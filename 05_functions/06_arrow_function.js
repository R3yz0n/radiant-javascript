// Traditional function syntax
add(1, 2);
function add(a, b) {
  return a + b;
}

// Arrow function syntax

const sum = (a, b, c) => {
  return a + b + c;
};
let result = sum(1, 2, 3);
// console.log(result);
result = sum(3, 3, 3);
// console.log(result);

// Find a square of a number using function with arrow syntax

const findSquare = () => {
  return;
};
